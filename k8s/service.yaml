apiVersion: v1
kind: Service
metadata:
  name: ds6-class-schedule-service
spec:
  selector:
    app: ds6-class-schedule-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
